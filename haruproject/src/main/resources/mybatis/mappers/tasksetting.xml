<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tasksetting">
	
	<!-- 업무 내용 수정 -->
	<update id="updateTaskContents" parameterType="taskvo">
		UPDATE task
		   SET task_contents =	#{taskContents}
		 WHERE task_no=#{taskNo}
	</update>

	<!-- 업무 라벨(color) 수정 -->
	<update id="updateTaskLabel" parameterType="map">
		UPDATE task
		   SET task_label = #{color}
		 WHERE task_no = #{taskNo}
	</update>



  
  <!--
  project > tasklist > task > comment
  				                  > file
  				                  > checklist
  				                  > tagtask
  				                  > taskuser
  				                  > comment
  select task title,start,end,tag,desc,rabels,checklisk,files,comments
  -->
	
</mapper>
